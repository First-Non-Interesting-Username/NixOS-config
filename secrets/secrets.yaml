# DO NOT COMMIT THIS FILE UNENCRYPTED!
# After adding your age public key to .sops.yaml, encrypt with:
#   sops --encrypt --in-place secrets/secrets.yaml
#
# To edit after encryption:
#   sops secrets/secrets.yaml

# User login password (hashed format for hashedPasswordFile)
# Generate with: mkpasswd -m sha-512
sudo_password: "REPLACE_WITH_HASHED_PASSWORD"

# Factorio.com account token for mod downloads
factorio_token: "REPLACE_WITH_FACTORIO_TOKEN"

# GitHub Personal Access Token
github_pat: "REPLACE_WITH_GITHUB_PAT"
