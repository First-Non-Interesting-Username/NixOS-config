_: {
  flake = {
    nixosModules.CHANGEME = {config, ...}: {
      options.custom.CHANGEME = {
        # Create options here
      };

      config = {
        # Add config here
      };
    };
  };
}
