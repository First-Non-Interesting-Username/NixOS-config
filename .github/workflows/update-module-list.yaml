name: Update Module List

on:
  push:
    branches:
      - main
    paths:
      - "modules/configuration/**/README.md"
      - "docs/HEADER.md"
  workflow_dispatch:
  schedule:
    - cron: "0 16 * * 5"

permissions:
  contents: read

jobs:
  update-list:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Generate module-list.md
        run: |
          # IMPORTANT: Update this list whenever a new module README is added
          READMES=(
            "modules/configuration/applications/README.md"
            "modules/configuration/desktop/README.md"
            "modules/configuration/development/README.md"
            "modules/configuration/iso/README.md"
            "modules/configuration/services/README.md"
            "modules/configuration/system/README.md"
            "modules/configuration/user/README.md"
          )

          cat /dev/null > docs/module-list.md

          cat docs/HEADER.md >> docs/module-list.md
          echo -e "\n" >> docs/module-list.md

          for readme in "${READMES[@]}"; do
            if [ -f "$readme" ]; then
              cat "$readme" >> docs/module-list.md
              echo -e "\n" >> docs/module-list.md
            fi
          done

      - name: Format with Prettier
        run: npx prettier --write docs/module-list.md

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8
        with:
          commit-message: "docs: update module-list.md"
          title: "docs: update module-list.md"
          body: |
            This is an automated PR to update `docs/module-list.md` based on changes in module READMEs.

            **Reminder:** Remember to update the hardcoded list in [this workflow file](.github/workflows/update-module-list.yaml).
          branch: auto-update-module-list
          delete-branch: true
